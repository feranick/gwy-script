2012-06-17b
Re-enabled port cleanup (it was commented out to allow debugging).

2012-06-17
Fixed a bug that prevented the addition of a new pixmap file (and therefore preventing compilation).

2012-05-10
The scripts detects the presence of patches for version 2.26 and disables them for version 2.27 and newer.

2011-12-19
Fixed several minor bugs, in particular in building stable versions from upstream.

2011-12-06b
Removed hardcoded condition for Portfile, so that the script doesn't fail if there is a revision bump from macports. 

2011-12-06
dmg packages are no longer built by default, but only when the --dmg argument is passed.

2011-12-05
First Release
